This is just a text file that Chris will update to document the design process.

Our first design was to acheive a random walk, where it picks an open direction, walks in that direction for X meters, then rotates to another direction and walks again
